Projekt wykorzystuje Flux, Helm, Kubernetes oraz FluxCLI.
Wszystko wykonuje się automatycznie lub za pomocą komendy fluxctl sync --k8s-fwd-ns flux, która synchronizuje nasz POD z repozytorium GitHuba.
